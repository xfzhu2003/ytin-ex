
to say or to chat or ... have such properties:

in the buffer of packet,

begin with a ASCII COLOR string;

the second line begin with a no ASCII COLOR string;(i don't know if
zmud can input a ascii color string)

end of a ASCII COLOR string(one line)
